# Use root/example as user/password credentials
version: '2'

services:

  mongo:
    image: mongo
    container_name: mongo

  mongo-express:
    image: mongo-express
    ports:
      - 8081:8081
